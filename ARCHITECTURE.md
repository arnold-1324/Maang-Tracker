# Interview Platform - Architecture & System Design

## 🏗️ System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                     CLIENT LAYER (Browser)                         │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │ Web UI: interview.html                                       │  │
│  │ - Code Editor (Dark Theme)                                  │  │
│  │ - Problem Viewer                                            │  │
│  │ - Real-time Chat Panel                                      │  │
│  │ - Test Results Display                                      │  │
│  │ - Friday Countdown Timer                                    │  │
│  └──────────────────────────────────────────────────────────────┘  │
└────────────┬──────────────────────────────────────────────┬────────┘
             │                                              │
      REST API (HTTP)                            WebSocket (Real-time)
             │                                              │
┌────────────▼──────────────────────────────────────────────▼────────┐
│                    APPLICATION LAYER (Flask)                       │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │ interview_routes.py (420+ lines)                            │  │
│  │ - 15+ REST Endpoints                                        │  │
│  │ - WebSocket Handlers                                        │  │
│  │ - Session Management                                        │  │
│  │ - Error Handling                                            │  │
│  └──────────────────────────────────────────────────────────────┘  │
└───┬──────────┬──────────────┬──────────────┬──────────────┬────────┘
    │          │              │              │              │
    ▼          ▼              ▼              ▼              ▼
┌────────┐ ┌────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│ Engine │ │Compiler│ │Scheduler │ │Chat      │ │Analytics │
│        │ │        │ │          │ │Handler   │ │         │
└────┬───┘ └───┬────┘ └────┬─────┘ └────┬─────┘ └────┬────┘
     │         │           │            │            │
     ▼         ▼           ▼            ▼            ▼
┌────────────────────────────────────────────────────────────┐
│           CORE LOGIC LAYER (Python Modules)                │
│                                                             │
│ ┌──────────────────┐  ┌──────────────────┐                │
│ │ simulation_engine│  │ compiler.py      │                │
│ │                  │  │                  │                │
│ │ - Interview Mgmt │  │ - Multi-language │                │
│ │ - Problem DB     │  │ - Sandbox Exec   │                │
│ │ - Session Store  │  │ - Test Validator │                │
│ │ - Chat History   │  │ - Complexity     │                │
│ │ - Scoring        │  │   Analysis       │                │
│ └──────────────────┘  └──────────────────┘                │
│                                                             │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ scheduler.py (400+ lines)                            │   │
│ │                                                       │   │
│ │ - Friday 3 PM Scheduling                            │   │
│ │ - Recurring Programs (12 weeks)                      │   │
│ │ - Smart Reminders (24h, 1h, 5m)                      │   │
│ │ - Countdown Calculation                             │   │
│ │ - Interview Status Management                       │   │
│ └──────────────────────────────────────────────────────┘   │
└────────────────┬──────────────────────────────────────────┘
                 │
                 │ (SQL Queries)
                 ▼
┌──────────────────────────────────────────────────────────┐
│            DATA PERSISTENCE LAYER                        │
│                                                          │
│ SQLite Database: memory/interview.db                     │
│                                                          │
│ Tables:                                                  │
│ ┌─────────────────────────────────────────────────────┐  │
│ │ interview_sessions      Chat History                │  │
│ │ code_submissions        Scheduled Interviews        │  │
│ │ interview_chat          Interview Reminders         │  │
│ │ interview_metrics       Interview Preferences       │  │
│ └─────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagram

### Coding Interview Flow

```
User Starts Interview
       │
       ▼
  Session Created ────► interview_sessions table
       │
       ├─► Load Problem ────► interview_database.json
       │         │
       │         ▼
       │    Display Problem
       │
       ▼
  User Writes Code
       │
       ▼
  Submit Code ────► CodeCompiler
       │                │
       │                ├─► Syntax Check
       │                │
       │                ├─► Compile (if needed)
       │                │
       │                ├─► Execute Code
       │                │
       │                ├─► Run Test Cases
       │                │
       │                ▼
       │         Test Results
       │                │
       ▼                ▼
  Store Submission ◄── code_submissions table
       │
       ├─► Display Results
       │
       ├─► AI Feedback
       │
       ├─► Store Chat ────► interview_chat table
       │
       ▼
  Repeat or End Interview
       │
       ▼
  Calculate Score ────► interview_metrics table
       │
       ▼
  Show Performance Summary
```

---

## 🔗 Component Interaction Diagram

```
┌─────────────────────┐
│  Web Browser        │
│  (interview.html)   │
└────────┬────────────┘
         │ HTTP REST API
         │ WebSocket
         │
┌────────▼────────────────────────────────┐
│      Flask Application                   │
│  (interview_routes.py)                   │
│                                          │
│  ┌────────────┐    ┌────────────┐      │
│  │GET /problem│    │POST /submit│      │
│  └────────────┘    └────────────┘      │
│  ┌────────────┐    ┌────────────┐      │
│  │GET /schedule      │POST /schedule     │
│  └────────────┘    └────────────┘      │
└────┬──────────────────────┬──────────────┘
     │                      │
     │                      │
┌────▼──────────────┐  ┌───▼─────────────────┐
│ InterviewEngine    │  │ CodeCompiler        │
│                    │  │                     │
│ - Load problems    │  │ - Execute code      │
│ - Create sessions  │  │ - Run tests         │
│ - Store chat       │  │ - Analyze           │
│ - Calculate score  │  │   complexity        │
└────┬──────────────┘  └───┬─────────────────┘
     │                      │
     │        ┌─────────────┤
     │        │             │
     ▼        ▼             │
┌──────────────────────┐    │
│ InterviewScheduler   │    │
│                      │    │
│ - Schedule Fridays   │    │
│ - Manage reminders   │    │
│ - Track interviews   │    │
└────┬─────────────────┘    │
     │                      │
     │    ┌─────────────────┘
     │    │
     ▼    ▼
┌─────────────────────────────────┐
│ SQLite Database                 │
│ (memory/interview.db)           │
│                                 │
│ - Sessions, submissions, chat   │
│ - Schedule, reminders, metrics  │
└─────────────────────────────────┘
```

---

## 📊 Request Flow (Sequence Diagram)

### Typical Coding Interview Session

```
User Browser          Flask API          Interview Engine      Database
    │                    │                     │                 │
    │ POST /session/create                     │                 │
    ├───────────────────►│                     │                 │
    │                    │                     │                 │
    │                    │ create_session()    │                 │
    │                    ├────────────────────►│                 │
    │                    │                     │                 │
    │                    │                     │ INSERT session  │
    │                    │                     ├────────────────►│
    │                    │                     │                 │
    │                    │◄────────────────────┤                 │
    │◄───────────────────┤                     │                 │
    │ {session_id: 1}    │                     │                 │
    │                    │                     │                 │
    │ GET /problem/two-sum                     │                 │
    ├───────────────────►│                     │                 │
    │                    │ load from cache     │                 │
    │                    │                     │                 │
    │◄───────────────────┤                     │                 │
    │ {problem details}  │                     │                 │
    │                    │                     │                 │
    │ [User writes code] │                     │                 │
    │                    │                     │                 │
    │ POST /session/1/submit-code              │                 │
    ├───────────────────►│                     │                 │
    │  {code, language}  │                     │                 │
    │                    │                     │                 │
    │                    │ submit_code()       │                 │
    │                    ├────────────────────►│                 │
    │                    │                     │                 │
    │                    │                     │ Execute code    │
    │                    │                     │ Run tests       │
    │                    │                     │                 │
    │                    │                     │ INSERT submission
    │                    │                     ├────────────────►│
    │                    │                     │                 │
    │                    │◄────────────────────┤                 │
    │◄───────────────────┤                     │                 │
    │ {test_results}     │                     │                 │
    │                    │                     │                 │
    │ [Chat with AI]     │                     │                 │
    │ WebSocket: send_message                  │                 │
    ├───────────────────►│                     │                 │
    │  {message}         │                     │                 │
    │                    │ add_chat_message()  │                 │
    │                    ├────────────────────►│                 │
    │                    │                     │                 │
    │                    │                     │ INSERT message  │
    │                    │                     ├────────────────►│
    │                    │                     │                 │
    │◄───────────────────┤ [WebSocket]         │                 │
    │ {ai_response}      │                     │                 │
    │                    │                     │                 │
    │ POST /session/1/end                      │                 │
    ├───────────────────►│                     │                 │
    │  {score: 85}       │                     │                 │
    │                    │                     │                 │
    │                    │ end_session()       │                 │
    │                    ├────────────────────►│                 │
    │                    │                     │                 │
    │                    │                     │ UPDATE session  │
    │                    │                     ├────────────────►│
    │                    │                     │                 │
    │◄───────────────────┤                     │                 │
    │ {score_result}     │                     │                 │
    │                    │                     │                 │
```

---

## 🎯 Module Dependencies

```
┌─────────────────────────────────────────┐
│         interview/__init__.py            │
│  (Public API - Exports all classes)     │
└─────────────┬───────────────────────────┘
              │
      ┌───────┼───────┐
      │       │       │
      ▼       ▼       ▼
  ┌────┐  ┌────┐  ┌────┐
  │ SE │  │ CC │  │ IS │
  └────┘  └────┘  └────┘
   
SE = simulation_engine.py
CC = compiler.py
IS = scheduler.py

ui/interview_routes.py
    │
    ├─► imports InterviewSimulationEngine
    ├─► imports CodeCompiler
    ├─► imports InterviewScheduler
    │
    └─► Flask routes (HTTP API)
        └─► WebSocket handlers
            └─► Real-time communication
```

---

## 💾 Data Model

```
interview_sessions
├─ id (PK)
├─ user_id
├─ mode (coding|design|behavioral)
├─ company_role
├─ problem_id
├─ start_time
├─ end_time
├─ duration_minutes
├─ status
├─ score
└─ feedback

code_submissions
├─ id (PK)
├─ session_id (FK)
├─ user_id
├─ code
├─ language
├─ submitted_at
├─ test_passed
├─ test_failed
└─ execution_time_ms

interview_chat
├─ id (PK)
├─ session_id (FK)
├─ speaker (user|ai)
├─ message
└─ timestamp

scheduled_interviews
├─ id (PK)
├─ user_id
├─ scheduled_time
├─ mode
├─ difficulty
├─ company_role
├─ status
├─ created_at
├─ completed_at
├─ cancelled_at
├─ score
└─ feedback

interview_reminders
├─ id (PK)
├─ interview_id (FK)
├─ user_id
├─ reminder_time
├─ sent
└─ sent_at

interview_preferences
├─ user_id (PK)
├─ preferred_time
├─ preferred_day
├─ preferred_mode
├─ frequency
├─ timezone
├─ email
└─ notifications_enabled

interview_metrics
├─ id (PK)
├─ user_id
├─ mode
├─ difficulty
├─ problems_solved
├─ problems_attempted
├─ avg_score
├─ total_hours
└─ last_updated
```

---

## 🚀 Deployment Architecture

```
┌────────────────────────────────────┐
│      Docker Compose Setup          │
│                                    │
│  ┌──────────────────────────────┐  │
│  │ dashboard (Port 5100)        │  │
│  │ - Flask Application          │  │
│  │ - Interview Routes           │  │
│  │ - WebSocket Server           │  │
│  └──────────────────────────────┘  │
│                                    │
│  ┌──────────────────────────────┐  │
│  │ sqlite (Internal)            │  │
│  │ - Interview Database         │  │
│  │ - Persistent Storage         │  │
│  └──────────────────────────────┘  │
│                                    │
│  ┌──────────────────────────────┐  │
│  │ Volume Mounts                │  │
│  │ - ./memory → /app/memory     │  │
│  │ - ./data → /app/data         │  │
│  └──────────────────────────────┘  │
└────────────────────────────────────┘
         │
         ├─► Network: maang-network
         │
         └─► Accessible at:
             http://localhost:5100/interview
```

---

## 🔐 Security Architecture

```
┌─────────────────────────────────────┐
│       Input Validation              │
│  - All endpoints validate input     │
│  - Type checking                    │
│  - Length limits                    │
└─────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────┐
│    Code Execution Sandbox           │
│  - Timeout (30s)                    │
│  - Memory limit (512MB)             │
│  - No system access                 │
│  - Subprocess isolation             │
└─────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────┐
│   SQL Injection Prevention          │
│  - Parameterized queries            │
│  - No string concatenation          │
│  - ORM safe patterns                │
└─────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────┐
│    Error Message Sanitization       │
│  - No sensitive data in errors      │
│  - Generic user messages            │
│  - Detailed logs server-side        │
└─────────────────────────────────────┘
```

---

**Architecture Version:** 1.0  
**Last Updated:** November 16, 2025  
**Status:** Production Ready ✅
